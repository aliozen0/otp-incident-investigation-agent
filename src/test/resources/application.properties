# Tests assert the deterministic docs/15 fixture numbers, so the tool layer reads the in-code
# dataset instead of the seeded operational tables. The DB-backed path has its own tests
# (JdbcOperationalDataToolsTest) that seed and query real rows.
# NOTE: .properties, not .yml — a test application.yml would shadow the main one entirely.
otp-sentinel.operations.source=fixture
otp-sentinel.operations.history-hours=1
