package com.example.otpsentinel.agent;

/**
 * Deterministic tool-budget mode for one investigation (docs M11 item 5). Never left to the model's
 * own initiative.
 */
public enum InvestigationMode {
  QUICK,
  THOROUGH
}
